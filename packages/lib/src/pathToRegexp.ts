import {
    MatchResult
} from './types'

export function pathToRegexp(path: string){

}

export function matchPath(
    pattern: string,
    pathname: string,
    options: {
        exact?: boolean
    } = {}
):MatchResult | null{
    return {

    } as MatchResult
}
